/**
 * 로그인 (/login)
 * Supabase Auth — 카카오 로그인 우선 (Phase 1에서 구현)
 */
export default function LoginPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center bg-[#EBF2FD] px-6">
      <div className="mx-auto w-full max-w-md text-center">
        <h1 className="text-2xl font-bold text-[#1A56DB]">로그인</h1>
        <p className="mt-2 text-slate-500">카카오 로그인 (구현 예정)</p>
      </div>
    </main>
  );
}
